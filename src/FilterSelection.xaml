<Window x:Class="BackupAssistant.FilterSelection"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Choose folders to back up" Icon="/assets/icon.ico"
        WindowStartupLocation="CenterScreen"
        Height="450" Width="500">
    <Window.Background>
        <SolidColorBrush Color="WhiteSmoke"/>
    </Window.Background>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="10"
                   TextWrapping="Wrap"
                   Text="Check the subfolders you want to include in the backup. Unchecked subfolders will not be backed up."/>

        <ListBox Grid.Row="1" Grid.Column="0"
                 ItemsSource="{Binding FilterItems}"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding IsChecked}"/>
                        <TextBlock Text="{Binding Path}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Grid.Column="0"
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Apply" Width="85" Click="Apply_Click"/>
            <Button Content="Cancel" Width="85" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
